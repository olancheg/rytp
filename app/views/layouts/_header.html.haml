.container
  .last_new
    %label Последняя новость:
    = link_to last_new.title.truncate(35), news_path(last_new) if last_new
  .contest
    - if last_contest
      = link_to last_contest do
        конкурс (#{last_contest.approved_poops(can?(:reject, Poop)).count})
    - else
      конкурсов пока нет
  .auth
    = render 'layouts/auth_block'
