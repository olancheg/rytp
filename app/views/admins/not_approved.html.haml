= set_title :title => 'новые пупы'
#not_approved_poops
  - if @poops.empty?
    .empty Нет ни одного видео.
  - else
    = render :partial => 'poops/poop', :collection => @poops
#navigation
  = will_paginate @poops
