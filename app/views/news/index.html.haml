%script $('menu_news').addClassName('active');
= set_title :title => 'редактирование новостей'
%h1 Listing news
%table.news
  %tr
    %th Title
    %th
    %th
    %th
  - @news.each do |news|
    %tr
      %td= news.title
      %td= link_to 'Show', news
      %td= link_to 'Edit', edit_news_path(news)
      %td= link_to 'Destroy', news, :confirm => 'Вы уверены?', :method => :delete
%br/
= link_to 'Create New', new_news_path
