= set_title :title => 'поиск'
= render :partial => 'poops/poop', :collection => @poops
- if @poops.empty?
  .empty Ваш запрос не дал результатов.

= content_for :ad_block_1 do
  = render :partial => '/ad/before_footer'
